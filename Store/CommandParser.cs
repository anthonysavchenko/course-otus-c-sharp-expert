namespace Store;

public static class CommandParser
{
  public static ParsedRequest Parse(ReadOnlySpan<byte> request)
  {
    var parsedRequest = new ParsedRequest();

    return parsedRequest;
  }
}
